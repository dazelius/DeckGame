# Shadow Deck CSS 가이드

## 📁 파일 구조

```
├── styles.css          # 메인 스타일 (레이아웃, 전투 UI, 유틸리티)
├── card-styles.css     # 카드 전용 디자인 시스템
├── responsive.css      # 반응형 & 유틸리티 클래스
├── map.css             # 던전 맵 스타일
├── town.css            # 마을 화면 스타일
└── title.css           # 타이틀 화면 스타일
```

---

## 🎨 CSS 변수 (Design Tokens)

모든 변수는 `styles.css`의 `:root`에 정의되어 있습니다.

### 색상

```css
/* 배경 */
--bg-primary: #0d0d12;
--bg-secondary: #151520;
--bg-tertiary: #1a1a28;
--bg-card: #1a1815;

/* 강조색 */
--accent-crimson: #dc3545;
--accent-gold: #f4d03f;
--accent-azure: #3498db;
--accent-emerald: #27ae60;
--accent-violet: #9b59b6;

/* 텍스트 */
--text-bright: #f5f5f5;
--text-normal: #c8c8d0;
--text-dim: #6a6a7a;
```

### 카드 타입 색상

```css
--card-attack: #ef4444;      /* 공격 카드 - 빨강 */
--card-skill: #3b82f6;       /* 스킬 카드 - 파랑 */
--card-power: #a855f7;       /* 파워 카드 - 보라 */
--card-status: #6b7280;      /* 상태이상 - 회색 */
--card-curse: #4c1d95;       /* 저주 - 어두운 보라 */
```

### 크기

```css
--card-width: 178px;
--card-height: 259px;
--card-cost-size: 34px;
--topbar-height: 50px;
```

### 간격

```css
--spacing-xs: 4px;
--spacing-sm: 8px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 32px;
```

### Z-Index 레이어

```css
--z-base: 1;
--z-card: 10;
--z-card-hover: 100;
--z-topbar: 9999;
--z-modal: 10000;
--z-overlay: 99999;
```

### 트랜지션

```css
--transition-fast: 0.15s ease;
--transition-normal: 0.3s ease;
--transition-slow: 0.5s ease;
```

---

## 📋 파일별 역할

### styles.css
- **역할**: 메인 스타일시트
- **포함 내용**:
  - CSS 변수 정의 (`:root`)
  - 전역 리셋 & 기본 스타일
  - TopBar, 레이아웃
  - 전투 UI (HP바, 에너지, 손패)
  - 버프/디버프 표시
  - 애니메이션 & 효과
  - 반응형 오버라이드

### card-styles.css
- **역할**: 카드 전용 디자인 시스템
- **포함 내용**:
  - 기본 카드 스타일 (고대 기억 테마)
  - 카드 내부 구조 (헤더, 코스트, 아이콘, 설명)
  - 타입별 스타일 (공격/스킬/파워/저주)
  - 희귀도별 글로우 효과
- **의존성**: `styles.css`의 CSS 변수 필요

### responsive.css
- **역할**: 반응형 디자인
- **포함 내용**:
  - 브레이크포인트별 스타일
  - 유틸리티 클래스 (`.mobile-only`, `.desktop-only`)
  - 터치 디바이스 최적화
  - 접근성 (움직임 감소 모드)

### map.css
- **역할**: 던전 맵 화면
- **포함 내용**:
  - 맵 레이아웃
  - 방 노드 스타일
  - 연결선
  - 현재 위치 표시

### town.css
- **역할**: 마을 화면
- **포함 내용**:
  - 마을 레이아웃
  - NPC 인터랙션
  - 대장장이 업그레이드 UI

---

## ⚠️ 주의사항

### 1. `!important` 사용 금지

```css
/* ❌ 나쁨 */
.card:hover {
    transform: scale(1.1) !important;
}

/* ✅ 좋음 - 선택자 구체화 */
.hand .card:hover {
    transform: scale(1.1);
}
```

### 2. CSS 변수 활용

```css
/* ❌ 나쁨 - 하드코딩 */
.button {
    background: #ef4444;
    transition: 0.3s ease;
}

/* ✅ 좋음 - 변수 사용 */
.button {
    background: var(--card-attack);
    transition: var(--transition-normal);
}
```

### 3. 선택자 구체성 규칙

```
낮음 ← → 높음
.card < .hand .card < body .hand .card
```

특정 컨텍스트에서 오버라이드 필요 시 부모 선택자 추가:

```css
/* 전투 중 손패 카드 */
.hand .card:hover { ... }

/* 보상 선택 카드 */
.ds-card-choices .ds-reward-full-card { ... }

/* 맵의 방 노드 */
.room-minimap .minimap-room.cleared { ... }
```

---

## 🔧 새 스타일 추가 시

### 1. 적절한 파일 선택
- 카드 관련 → `card-styles.css`
- 맵 관련 → `map.css`
- 마을 관련 → `town.css`
- 그 외 → `styles.css`

### 2. 기존 변수 확인
```css
/* 새 색상이 필요한가? 기존 변수 확인 */
color: var(--accent-gold);  /* O */
color: #f4d03f;             /* X - 이미 변수 있음 */
```

### 3. 네이밍 컨벤션
```css
/* 컴포넌트-요소-상태 */
.card-header { }
.card-header-active { }

/* BEM 스타일 권장 */
.reward-card { }
.reward-card__title { }
.reward-card--selected { }
```

---

## 🐛 디버깅 팁

### 스타일이 적용 안 될 때

1. **개발자 도구에서 확인**
   - 다른 스타일이 덮어쓰고 있는지
   - 선택자가 맞는지

2. **선택자 구체성 높이기**
   ```css
   /* 안 먹힐 때 */
   .card { }
   
   /* 부모 추가 */
   .hand .card { }
   ```

3. **로드 순서 확인**
   - `index.html`에서 CSS 로드 순서
   - 나중에 로드된 파일이 우선

### 반응형 문제

1. **브레이크포인트 확인**
   ```
   mobile-sm: ~480px
   mobile: 481~768px
   tablet: 769~1024px
   desktop: 1025px~
   ```

2. **`responsive.css` 변수 확인**
   ```css
   --card-width, --card-height 등
   ```

---

## 📝 변경 이력

| 날짜 | 내용 |
|------|------|
| 2024-12-24 | CSS 리팩토링 - !important 100% 제거 |
| 2024-12-24 | CSS 변수 시스템 확장 |
| 2024-12-24 | 카드 스타일 분리 (card-styles.css) |

